package logic.model.domain;

// Enum che rappresenta lo stato di una recensione in una sessione
// di tutoraggio: non iniziata, pendente, completata
public enum ReviewStatus {
    NOT_STARTED,
    PENDING,
    COMPLETE
}
